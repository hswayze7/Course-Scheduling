@model IEnumerable<CourseScheduling.Models.Course>

<h2>Available Courses</h2>

@if (ViewBag.StudentName != null)
{
    <p>Welcome, @ViewBag.StudentName!</p>
}

<h3>Your Enrolled Courses:</h3>
@if (ViewBag.Enrollments != null && ((List<CourseScheduling.Models.Enrollment>)ViewBag.Enrollments).Count > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th>Course Code</th>
                <th>Course Name</th>
                <th>Credits</th>
                <th>Time</th>
                <th>Enrollment Date</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var enrollment in ViewBag.Enrollments)
            {
                <tr>
                    <td>@enrollment.Course.CourseCode</td>
                    <td>@enrollment.Course.CourseName</td>
                    <td>@enrollment.Course.Credits</td>
                    <td>@enrollment.Course.Time</td>
                    <td>@enrollment.EnrollmentDate.ToShortDateString()</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>You are not enrolled in any courses yet.</p>
}


<table class="table">
    <thead>
        <tr>
            <th>Course Code</th>
            <th>Course Name</th>
            <th>Credits</th>
            <th>Time</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var course in Model)
        {
            <tr>
                <td>@course.CourseCode</td>
                <td>@course.CourseName</td>
                <td>@course.Credits</td>
                <td>@course.Time</td>
                <td>
                    <form method="post" asp-action="Enroll" asp-route-courseId="@course.CourseId">
                        <input type="hidden" name="studentId" value="1" /> <!-- For now, static student ID -->
                        <button type="submit" class="btn btn-primary">Enroll</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
